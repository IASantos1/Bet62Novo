From fbbda54a52f59dab435064d9a2db13f0bf503781 Mon Sep 17 00:00:00 2001
From: IASantos1 <IASantos1@users.noreply.github.com>
Date: Thu, 4 Jun 2026 00:34:51 +0000
Subject: [PATCH 8/9] feat: Fazer Pull Repository

Co-authored-by: traeagent <traeagent@users.noreply.github.com>
---
 ...x-avoid-live-freeze-when-WS-is-stale.patch | 69 +++++++++++++++++++
 1 file changed, 69 insertions(+)
 create mode 100644 patches/0001-fix-avoid-live-freeze-when-WS-is-stale.patch

diff --git a/patches/0001-fix-avoid-live-freeze-when-WS-is-stale.patch b/patches/0001-fix-avoid-live-freeze-when-WS-is-stale.patch
new file mode 100644
index 0000000..6047431
--- /dev/null
+++ b/patches/0001-fix-avoid-live-freeze-when-WS-is-stale.patch
@@ -0,0 +1,69 @@
+From 598535052a2934fa2803755f5f938de947ba2533 Mon Sep 17 00:00:00 2001
+From: IASantos1 <IASantos1@users.noreply.github.com>
+Date: Thu, 4 Jun 2026 00:34:23 +0000
+Subject: [PATCH] fix: avoid live freeze when WS is stale
+
+---
+ artifacts/api-server/src/routes/matches.ts | 11 ++++++-----
+ 1 file changed, 6 insertions(+), 5 deletions(-)
+
+diff --git a/artifacts/api-server/src/routes/matches.ts b/artifacts/api-server/src/routes/matches.ts
+index 45f091a..d32bcba 100644
+--- a/artifacts/api-server/src/routes/matches.ts
++++ b/artifacts/api-server/src/routes/matches.ts
+@@ -4169,6 +4169,7 @@ async function getVolleyballLive(): Promise<VolleyTournament[]> {
+ 
+ // ─── SportsAPI Pro V2 Live Fetch Functions ────────────────────────────────────
+ const V2_LIVE_MAX_STALE_MS = 30_000;
++const WS_PREFERRED_MAX_STALE_MS = 10_000;
+ 
+ /** Race V1 vs V2 HTTP — whichever resolves first wins. V1 = 1-2s, V2 = 3-5s. */
+ async function fetchLiveRace(v1Base: string, v2Base: string): Promise<SAPIV2Event[]> {
+@@ -4186,7 +4187,7 @@ async function fetchLiveRace(v1Base: string, v2Base: string): Promise<SAPIV2Even
+ 
+ async function getFootballLiveV2(): Promise<SAPIV2Event[]> {
+   const now = Date.now();
+-  if ((wsConnected.has("football") || v1WsConnected.has("football")) && footballLiveV2Cache) return footballLiveV2Cache;
++  if ((wsConnected.has("football") || v1WsConnected.has("football")) && footballLiveV2Cache && now - footballLiveV2FetchedAt < WS_PREFERRED_MAX_STALE_MS) return footballLiveV2Cache;
+   if (footballLiveV2Cache && now - footballLiveV2FetchedAt < CONFIG.LIVE_CACHE_TTL) return footballLiveV2Cache;
+   try {
+     const events = await fetchLiveRace(SAPI_V1_FOOTBALL, SAPI_V2_FOOTBALL);
+@@ -4208,7 +4209,7 @@ async function getFootballLiveV2(): Promise<SAPIV2Event[]> {
+ 
+ async function getBasketballLiveV2(): Promise<SAPIV2Event[]> {
+   const now = Date.now();
+-  if ((wsConnected.has("basketball") || v1WsConnected.has("basketball")) && basketballLiveV2Cache) return basketballLiveV2Cache;
++  if ((wsConnected.has("basketball") || v1WsConnected.has("basketball")) && basketballLiveV2Cache && now - basketballLiveV2FetchedAt < WS_PREFERRED_MAX_STALE_MS) return basketballLiveV2Cache;
+   if (basketballLiveV2Cache && now - basketballLiveV2FetchedAt < CONFIG.LIVE_CACHE_TTL) return basketballLiveV2Cache;
+   try {
+     const events = await fetchLiveRace(SAPI_V1_BASKETBALL, SAPI_V2_BASKETBALL);
+@@ -4230,7 +4231,7 @@ async function getBasketballLiveV2(): Promise<SAPIV2Event[]> {
+ 
+ async function getHockeyLiveV2(): Promise<SAPIV2Event[]> {
+   const now = Date.now();
+-  if ((wsConnected.has("hockey") || v1WsConnected.has("hockey")) && hockeyLiveV2Cache) return hockeyLiveV2Cache;
++  if ((wsConnected.has("hockey") || v1WsConnected.has("hockey")) && hockeyLiveV2Cache && now - hockeyLiveV2FetchedAt < WS_PREFERRED_MAX_STALE_MS) return hockeyLiveV2Cache;
+   if (hockeyLiveV2Cache && now - hockeyLiveV2FetchedAt < CONFIG.LIVE_CACHE_TTL) return hockeyLiveV2Cache;
+   try {
+     const resp = await fetch(`${SAPI_V2_HOCKEY}/live`, { signal: AbortSignal.timeout(9000), headers: sapiHeaders() });
+@@ -4256,7 +4257,7 @@ async function getHockeyLiveV2(): Promise<SAPIV2Event[]> {
+ 
+ async function getBaseballLiveV2(): Promise<SAPIV2Event[]> {
+   const now = Date.now();
+-  if ((wsConnected.has("baseball") || v1WsConnected.has("baseball")) && baseballLiveV2Cache) return baseballLiveV2Cache;
++  if ((wsConnected.has("baseball") || v1WsConnected.has("baseball")) && baseballLiveV2Cache && now - baseballLiveV2FetchedAt < WS_PREFERRED_MAX_STALE_MS) return baseballLiveV2Cache;
+   if (baseballLiveV2Cache && now - baseballLiveV2FetchedAt < CONFIG.LIVE_CACHE_TTL) return baseballLiveV2Cache;
+   try {
+     const resp = await fetch(`${SAPI_V2_BASEBALL}/live`, { signal: AbortSignal.timeout(9000), headers: sapiHeaders() });
+@@ -4282,7 +4283,7 @@ async function getBaseballLiveV2(): Promise<SAPIV2Event[]> {
+ 
+ async function getTennisLiveV2(): Promise<SAPIV2Event[]> {
+   const now = Date.now();
+-  if ((wsConnected.has("tennis") || v1WsConnected.has("tennis")) && tennisLiveV2Cache) return tennisLiveV2Cache;
++  if ((wsConnected.has("tennis") || v1WsConnected.has("tennis")) && tennisLiveV2Cache && now - tennisLiveV2FetchedAt < WS_PREFERRED_MAX_STALE_MS) return tennisLiveV2Cache;
+   if (tennisLiveV2Cache && now - tennisLiveV2FetchedAt < CONFIG.LIVE_CACHE_TTL) return tennisLiveV2Cache;
+   try {
+     // Race V1 vs V2 — tennis endpoint shape varies, handle both formats
+-- 
+2.43.0
+
-- 
2.43.0

