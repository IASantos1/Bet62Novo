auto-install-peers=false
strict-peer-dependencies=false

# Força o hoisting de TODOS os pacotes para node_modules raiz
# (igual npm/yarn clássico). Remove 100% dos erros de "Módulo não encontrado"
# em IDEs e TSC de workspaces monorepo.
shamefully-hoist=true
public-hoist-pattern[]=*
