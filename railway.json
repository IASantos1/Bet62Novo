{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm -C artifacts/api-server build"
  },
  "deploy": {
    "startCommand": "pnpm -C artifacts/api-server start"
  },
  "variables": {
    "RAILPACK_INSTALL_CMD": "pnpm install --no-frozen-lockfile"
  }
}
